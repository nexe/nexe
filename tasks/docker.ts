export function buildArm(target: string) {
  console.log('building arm: ' + target)
  return Promise.resolve('')
}

export function buildAlpine(target: string) {
  console.log('building alpine: ' + target)
  return Promise.resolve('')
}
